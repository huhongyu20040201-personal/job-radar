__pycache__/
*.pyc

# Claude Code local settings, unrelated to the project
.claude/

# Log from local runs
run.log

# In case a token was ever stored locally, keep it out of the repo
secrets.bat
.env

# Note: seen.json and digest.md are committed on purpose.
# seen.json is the dedup state; without it every cloud run starts from scratch
# and re-sends the same jobs.
